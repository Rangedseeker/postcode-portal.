
/* ===== THEME (adjust these to match brand) ===== */
:root{
  --brand: #1ea1f2;        /* header accent */
  --brand-dark: #167dbd;
  --white: #ffffff;
  --bg: #f4f7fb;           /* page background */
  --panel: #ffffff;
  --panel-border: #e4e8f0;
  --text: #222b3b;
  --muted: #64748b;
  --line: #d6dbe6;
  --focus: #2563eb;
  --subnav: #4a5568;       /* grey strip */
  --footer: #2f3b4a;
}

/* ===== BASE ===== */
*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  background:var(--bg);
  color:var(--text);
  font:16px/1.5 "Inter", system-ui, -apple-system, Segoe UI, Roboto, Arial;
}
a{color:inherit;text-decoration:none}

/* ===== HEADER ===== */
.header{
  display:flex; align-items:center; justify-content:space-between;
  gap:16px; padding:10px 16px; background:var(--white);
  border-bottom:1px solid var(--panel-border);
  position:sticky; top:0; z-index:100;
}
.header__brand{display:flex; align-items:center; gap:10px}
.logo{
  width:38px; height:38px; border-radius:8px;
  background:linear-gradient(0deg, var(--brand), var(--brand-dark));
}
.brand-text{font-weight:600; letter-spacing:.2px}
.header__right{display:flex; align-items:center; gap:12px}
.user-label{color:var(--muted); font-size:14px}
.header-icons{display:flex; gap:10px}
.icon{display:inline-flex; align-items:center; justify-content:center}

/* ===== TOP NAV (white) ===== */
.topnav{
  display:flex; flex-wrap:wrap; gap:8px; padding:8px 16px; background:var(--white);
  border-bottom:1px solid var(--panel-border);
}
.topnav a{
  padding:8px 10px; border-radius:8px; color:#344153;
}
.topnav a.active, .topnav a:hover{
  background:#eef4ff; border:1px solid #cfe1ff;
}

/* ===== SUB NAV (grey strip) ===== */
.subnav{
  background:var(--subnav); color:#fff; padding:8px 16px;
  border-bottom:1px solid rgba(255,255,255,.15);
}

/* ===== LAYOUT ===== */
.layout{max-width:1200px; margin:20px auto 40px; padding:0 16px}
.hero{
  min-height:140px;
  background:linear-gradient(180deg, #eaf4ff 0%, #f7fbff 100%);
  border:1px solid var(--panel-border);
  border-radius:12px;
}
.content{display:grid; gap:16px; margin-top:16px}
.card{
  background:var(--panel); border:1px solid var(--panel-border);
  border-radius:12px; padding:14px;
}
h2{font-size:18px; margin:0 0 12px}
label{display:block; font-size:12px; color:var(--muted); margin-bottom:6px}
input,select{
  width:100%; padding:10px 12px; border-radius:10px;
  border:1px solid var(--panel-border); background:#fff; color:var(--text); outline:none;
}
input:focus,select:focus{
  border-color:var(--focus); box-shadow:0 0 0 3px rgba(37,99,235,.15);
}
.topbar{display:grid; grid-template-columns:1fr 1fr; gap:12px}
.summary{margin-top:8px}
.small{font-size:12px}
.muted{color:var(--muted)}

.numbers-grid{display:grid; grid-template-columns:1fr 1fr; gap:12px}
.sum-card{
  background:#f9fbff; border:1px solid var(--panel-border);
  border-radius:12px; padding:12px;
}
.pill{
  display:inline-grid; grid-auto-flow:column; gap:8px; align-items:center;
  padding:8px 10px; border:1px solid var(--panel-border);
  border-radius:999px; background:#fafbff; color:var(--muted)
}

/* ===== FOOTER ===== */
.footer{
  background:var(--footer); color:#cbd5e1; text-align:center;
  padding:12px 16px; border-top:1px solid #22303c;
}

/* ===== RESPONSIVE ===== */
@media (max-width:900px){
  .topbar, .numbers-grid{grid-template-columns:1fr}
}
``
